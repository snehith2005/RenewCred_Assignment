NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Used only for server-side rendering (getServerSideProps) inside Docker,
# where "localhost" refers to this container, not the backend container.
# Not needed for `npm run dev` outside Docker.
API_INTERNAL_URL=http://backend:5000/api/v1
